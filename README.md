# 📧 Mail Server для sutulaya.lol

Полнофункциональный почтовый сервер на базе Docker с Postfix, Dovecot и Roundcube. Простая установка, легкое развертывание на любом сервере.

## ✨ Возможности

- 📨 **Отправка и получение почты** - Полноценный SMTP/IMAP/POP3 сервер
- 🌐 **Веб-интерфейс** - Roundcube webmail для доступа через браузер
- 🔒 **Безопасность** - TLS/SSL шифрование, DKIM, SPF, DMARC
- 👥 **Множество пользователей** - Неограниченное количество почтовых ящиков
- 📊 **База данных** - PostgreSQL для управления пользователями
- 💾 **Резервное копирование** - Автоматизированные скрипты бэкапа
- 🐳 **Docker** - Контейнеризация для простого развертывания
- 🚀 **Быстрая установка** - Один скрипт для полной настройки

## 📋 Требования

- Docker 20.10+
- Docker Compose 2.0+
- Доступ к DNS настройкам домена
- Сервер с публичным IP адресом
- Открытые порты: 25, 587, 465, 993, 143, 110, 995, 8080
- Минимум 2GB RAM
- Минимум 10GB свободного места

## 🚀 Быстрая установка

### Шаг 1: Клонируйте проект

```bash
git clone <repository-url>
cd mail-server
```

### Шаг 2: Настройте конфигурацию

```bash
cp .env.example .env
nano .env
```

Измените следующие параметры в файле `.env`:

```env
# Ваш домен
MAIL_DOMAIN=sutulaya.lol
HOSTNAME=mail.sutulaya.lol

# Пароль базы данных (обязательно смените!)
POSTGRES_PASSWORD=your_strong_password_here

# Email администратора
ADMIN_EMAIL=admin@sutulaya.lol
SSL_EMAIL=admin@sutulaya.lol
```

### Шаг 3: Запустите установку

```bash
chmod +x scripts/*.sh
./scripts/setup.sh
```

Скрипт автоматически:
- Создаст необходимые директории
- Сгенерирует SSL сертификаты (самоподписанные для тестирования)
- Скачает Docker образы
- Запустит все сервисы
- Создаст базу данных с тестовым пользователем

### Шаг 4: Настройте DNS записи

См. подробную инструкцию в [docs/dns-records.md](docs/dns-records.md)

Минимально необходимые записи:

```dns
mail.sutulaya.lol.              A       YOUR_SERVER_IP
sutulaya.lol.                   MX      10 mail.sutulaya.lol.
sutulaya.lol.                   TXT     "v=spf1 mx -all"
```

### Шаг 5: Сгенерируйте DKIM ключи

```bash
./scripts/generate-dkim.sh
```

Добавьте полученную DNS запись в настройки домена.

## 📖 Использование

### Веб-интерфейс (Roundcube)

Откройте в браузере:
```
http://your-server-ip:8080
```

**Учетные данные по умолчанию:**
- Email: `admin@sutulaya.lol`
- Пароль: `changeme`

⚠️ **Важно:** Смените пароль сразу после первого входа!

### Настройка почтового клиента

#### IMAP (для получения почты)
- Сервер: `mail.sutulaya.lol`
- Порт: `993`
- Безопасность: SSL/TLS
- Имя пользователя: `ваш-email@sutulaya.lol`
- Пароль: ваш пароль

#### SMTP (для отправки почты)
- Сервер: `mail.sutulaya.lol`
- Порт: `587`
- Безопасность: STARTTLS
- Аутентификация: Да
- Имя пользователя: `ваш-email@sutulaya.lol`
- Пароль: ваш пароль

### Управление пользователями

#### Добавить нового пользователя

```bash
./scripts/add-user.sh
```

Следуйте инструкциям на экране:
1. Введите email адрес
2. Введите пароль
3. Подтвердите пароль

#### Удалить пользователя

```bash
docker-compose exec postgres psql -U mailuser -d mailserver -c \
  "DELETE FROM virtual_users WHERE email='user@sutulaya.lol';"
```

#### Изменить пароль пользователя

```bash
# Сгенерировать хэш пароля
docker-compose exec dovecot doveadm pw -s BLF-CRYPT

# Обновить в базе данных
docker-compose exec postgres psql -U mailuser -d mailserver -c \
  "UPDATE virtual_users SET password='HASH' WHERE email='user@sutulaya.lol';"
```

#### Список всех пользователей

```bash
docker-compose exec postgres psql -U mailuser -d mailserver -c \
  "SELECT email, active, created_at FROM virtual_users;"
```

### Управление доменами

#### Добавить новый домен

```bash
docker-compose exec postgres psql -U mailuser -d mailserver -c \
  "INSERT INTO virtual_domains (name, active) VALUES ('newdomain.com', true);"
```

#### Список доменов

```bash
docker-compose exec postgres psql -U mailuser -d mailserver -c \
  "SELECT * FROM virtual_domains;"
```

### Алиасы (перенаправление почты)

#### Создать алиас

```bash
docker-compose exec postgres psql -U mailuser -d mailserver -c \
  "INSERT INTO virtual_aliases (domain_id, source, destination, active) \
   VALUES (1, 'info@sutulaya.lol', 'admin@sutulaya.lol', true);"
```

#### Список алиасов

```bash
docker-compose exec postgres psql -U mailuser -d mailserver -c \
  "SELECT source, destination, active FROM virtual_aliases;"
```

## 🔧 Управление сервером

### Просмотр логов

```bash
# Все логи
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f postfix
docker-compose logs -f dovecot
docker-compose logs -f roundcube
```

### Перезапуск сервисов

```bash
# Перезапустить все
docker-compose restart

# Перезапустить конкретный сервис
docker-compose restart postfix
docker-compose restart dovecot
```

### Остановка и запуск

```bash
# Остановить все сервисы
docker-compose down

# Запустить все сервисы
docker-compose up -d

# Запустить с пересборкой
docker-compose up -d --build
```

### Обновление

```bash
# Остановить сервисы
docker-compose down

# Обновить образы
docker-compose pull

# Запустить с новыми образами
docker-compose up -d
```

## 💾 Резервное копирование и восстановление

### Создать бэкап

```bash
./scripts/backup.sh
```

Бэкап включает:
- База данных PostgreSQL
- Все почтовые ящики
- DKIM ключи
- SSL сертификаты
- Файлы конфигурации

Бэкапы сохраняются в `backups/YYYYMMDD_HHMMSS/`

### Восстановить из бэкапа

```bash
./scripts/restore.sh backups/20240130_120000
```

## 🔒 Безопасность

### SSL/TLS сертификаты

#### Самоподписанные (для тестирования)

Генерируются автоматически при первом запуске.

#### Let's Encrypt (для продакшена)

```bash
# Установите certbot
apt-get install certbot

# Получите сертификат
certbot certonly --standalone -d mail.sutulaya.lol

# Скопируйте сертификаты
cp /etc/letsencrypt/live/mail.sutulaya.lol/fullchain.pem ssl/cert.pem
cp /etc/letsencrypt/live/mail.sutulaya.lol/privkey.pem ssl/key.pem

# Перезапустите сервисы
docker-compose restart postfix dovecot
```

#### Автоматическое обновление Let's Encrypt

Добавьте в crontab:

```bash
0 3 * * * certbot renew --quiet && cp /etc/letsencrypt/live/mail.sutulaya.lol/*.pem /path/to/ssl/ && docker-compose restart postfix dovecot
```

### Настройки безопасности

Все критические настройки находятся в `.env`:

```env
# Включить защиту от брутфорса
ENABLE_FAIL2BAN=1

# Включить антиспам
ENABLE_SPAMASSASSIN=1

# Отключить антивирус (ресурсоемко)
ENABLE_CLAMAV=0
```

### Файрвол

Откройте необходимые порты:

```bash
# Ubuntu/Debian с ufw
ufw allow 25/tcp    # SMTP
ufw allow 587/tcp   # Submission
ufw allow 465/tcp   # SMTPS
ufw allow 143/tcp   # IMAP
ufw allow 993/tcp   # IMAPS
ufw allow 110/tcp   # POP3
ufw allow 995/tcp   # POP3S
ufw allow 8080/tcp  # Webmail

# CentOS/RHEL с firewalld
firewall-cmd --permanent --add-port=25/tcp
firewall-cmd --permanent --add-port=587/tcp
firewall-cmd --permanent --add-port=465/tcp
firewall-cmd --permanent --add-port=993/tcp
firewall-cmd --reload
```

## 🧪 Тестирование

### Проверка отправки почты

```bash
# Из контейнера postfix
docker-compose exec postfix bash
echo "Test email" | mail -s "Test Subject" test@example.com
```

### Проверка получения почты

Отправьте письмо на `admin@sutulaya.lol` с внешнего почтового сервиса.

### Проверка SMTP аутентификации

```bash
telnet mail.sutulaya.lol 587
EHLO sutulaya.lol
STARTTLS
# После установки TLS соединения:
AUTH LOGIN
# Введите base64 закодированные логин и пароль
```

### Онлайн проверки

- **Mail-tester:** https://www.mail-tester.com/
- **MXToolbox:** https://mxtoolbox.com/SuperTool.aspx
- **DKIM Validator:** https://dkimvalidator.com/

## 🐛 Решение проблем

### Почта не отправляется

1. Проверьте логи Postfix:
   ```bash
   docker-compose logs postfix
   ```

2. Проверьте порт 25:
   ```bash
   telnet mail.sutulaya.lol 25
   ```

3. Проверьте DNS записи:
   ```bash
   dig MX sutulaya.lol
   ```

### Почта не получается

1. Проверьте MX запись:
   ```bash
   nslookup -type=mx sutulaya.lol
   ```

2. Проверьте логи Dovecot:
   ```bash
   docker-compose logs dovecot
   ```

3. Проверьте доступность портов:
   ```bash
   netstat -tulpn | grep -E ':(25|587|993)'
   ```

### Письма попадают в спам

1. Настройте SPF, DKIM, DMARC (см. [docs/dns-records.md](docs/dns-records.md))
2. Настройте PTR запись (обратитесь к хостинг-провайдеру)
3. Проверьте IP в черных списках: https://mxtoolbox.com/blacklists.aspx
4. Прогрейте почтовый сервер (начните с небольших объемов)

### Не могу войти в Roundcube

1. Проверьте статус контейнера:
   ```bash
   docker-compose ps roundcube
   ```

2. Проверьте логи:
   ```bash
   docker-compose logs roundcube
   ```

3. Проверьте подключение к базе данных:
   ```bash
   docker-compose exec postgres psql -U mailuser -d mailserver -c '\dt'
   ```

### База данных не запускается

1. Проверьте логи:
   ```bash
   docker-compose logs postgres
   ```

2. Проверьте права доступа:
   ```bash
   ls -la data/postgres/
   ```

3. Пересоздайте данные:
   ```bash
   docker-compose down
   rm -rf data/postgres
   docker-compose up -d
   ```

## 📁 Структура проекта

```
mail-server/
├── config/                 # Конфигурационные файлы
│   ├── postfix/           # Настройки Postfix
│   │   ├── main.cf
│   │   ├── master.cf
│   │   └── pgsql/
│   ├── dovecot/           # Настройки Dovecot
│   │   ├── dovecot.conf
│   │   └── dovecot-sql.conf.ext
│   ├── roundcube/         # Настройки Roundcube
│   │   └── config.inc.php
│   └── db/                # База данных
│       └── init.sql
├── scripts/               # Утилиты
│   ├── setup.sh          # Установка
│   ├── add-user.sh       # Добавление пользователя
│   ├── generate-dkim.sh  # Генерация DKIM
│   ├── backup.sh         # Резервное копирование
│   └── restore.sh        # Восстановление
├── docs/                  # Документация
│   └── dns-records.md    # Настройка DNS
├── data/                  # Данные (создается автоматически)
│   ├── postgres/         # База данных
│   ├── mail/             # Почтовые ящики
│   ├── dkim/             # DKIM ключи
│   └── roundcube/        # Данные Roundcube
├── ssl/                   # SSL сертификаты
├── docker-compose.yml     # Конфигурация Docker
├── .env                   # Переменные окружения
├── .env.example          # Пример конфигурации
├── .gitignore            # Git ignore
└── README.md             # Эта документация
```

## 🔄 Миграция на другой сервер

### На исходном сервере:

```bash
# 1. Создайте бэкап
./scripts/backup.sh

# 2. Скопируйте файлы проекта и бэкап на новый сервер
scp -r mail-server/ user@new-server:/path/to/mail-server/
```

### На новом сервере:

```bash
# 1. Установите Docker и Docker Compose
curl -fsSL https://get.docker.com | sh
apt-get install docker-compose-plugin

# 2. Перейдите в директорию проекта
cd /path/to/mail-server/

# 3. Восстановите из бэкапа
./scripts/restore.sh backups/YYYYMMDD_HHMMSS/

# 4. Обновите DNS записи на новый IP адрес

# 5. Готово!
```

## 📚 Дополнительные ресурсы

- [Postfix Documentation](http://www.postfix.org/documentation.html)
- [Dovecot Wiki](https://doc.dovecot.org/)
- [Roundcube Documentation](https://github.com/roundcube/roundcubemail/wiki)
- [Docker Documentation](https://docs.docker.com/)
- [Email Best Practices](https://www.rfc-editor.org/rfc/rfc5321.html)

## 🤝 Поддержка

Если у вас возникли проблемы:

1. Проверьте [Решение проблем](#-решение-проблем)
2. Просмотрите логи: `docker-compose logs -f`
3. Проверьте DNS записи: [docs/dns-records.md](docs/dns-records.md)

## 📝 Лицензия

Этот проект распространяется под лицензией MIT.

## ⚠️ Важные примечания

1. **Пароли:** Обязательно смените все пароли по умолчанию в файле `.env`
2. **SSL:** Используйте Let's Encrypt сертификаты для продакшена
3. **Бэкапы:** Регулярно создавайте резервные копии
4. **Обновления:** Следите за обновлениями Docker образов
5. **Мониторинг:** Настройте мониторинг работы сервера
6. **Логи:** Регулярно проверяйте логи на ошибки

## 🎉 Готово!

Ваш почтовый сервер готов к работе! Теперь вы можете:

- ✉️ Отправлять и получать письма
- 🌐 Использовать веб-интерфейс Roundcube
- 👥 Создавать неограниченное количество почтовых ящиков
- 🔒 Наслаждаться безопасной зашифрованной почтой
- 🚀 Легко мигрировать на другие серверы

**Наслаждайтесь собственным почтовым сервером!** 🎊
